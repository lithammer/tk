# Create tickets locally by default

tk creates new **Tickets** and **Epics** locally by default, even when a
**Primary Backend** is configured. Agents need a quick way to capture temporary
context, deferred decisions, and follow-ups without adding them to an upstream
issue tracker. **Promotion** is the curation step that creates Backend objects.
